# Development / evaluation environment for the PTCG battle agent.
#
# IMPORTANT: the SUBMITTED agent (ptcg_bot/ -> dist/main.py) needs NONE of these.
# It is pure standard library; tools/bundle.py freezes tuned weights to literals
# and drops `os`. These packages are only for loading data, running the engine
# locally, the test suite, figures, and submitting.
#
# Pinned to the versions this project was developed and validated against.

# Unit test runner (tests/). >=9.0.3 patches CVE-2025-71176.
pytest>=9.0.3,<10

# Kaggle CLI — download competition data + upload the submission (see SUBMIT.md).
# This machine authenticates via the new OAuth flow (~/.kaggle/credentials.json),
# which requires the 2.x CLI.
kaggle>=2.2

# Writeup figures (writeup/figures.py). Not needed by the agent or the engine.
matplotlib>=3.8

# --- Quality gate (dev-only; excluded from the bundled agent) ----------------
# Format / imports / lint / types + the pre-commit runner. tools/bundle.py ships
# a pure-stdlib dist/main.py, so NONE of these reach the submission. Run via the
# Makefile: `make format`, `make lint`, `make typecheck`, `make ci`.
ruff>=0.9,<1
black>=26.3.1,<27  # >=26.3.1 patches CVE-2026-32274
isort>=5.13,<6
mypy>=1.13,<2
pre-commit>=4.0,<5
bandit>=1.8,<2
pip-audit>=2.7,<3

# --- Simulator (schema now published; access rules-gated) -------------------
# As of 2026-07-01 the engine ships on the `pokemon-tcg-ai-battle` competition
# under `sample_submission/` (a Python `cg/` package: api.py/game.py/sim.py +
# native libcg.{so,dylib,dll}) and `ptcg_engine/`. It is vendored with the
# submission, NOT a pip package — so there is no pin here. Downloading it
# requires accepting that competition's rules (see ASSUMPTIONS.md / SUBMIT.md).
# Once downloaded, implement ptcg_bot/engine_adapter.py against cg/api.py and
# wire tools/verify_env.py to assert round-trip fidelity (plan §W2).

# Offline training of the learned evaluator (tools/train_eval.py). Dev-only —
# the shipped agent infers with pure stdlib (ptcg_bot/eval_weights.py literals).
numpy>=1.26,<3
