"""Evaluation & submission harness (NOT submitted with the agent).

Ports the Orbit Wars discipline to PTCG: verify_env (engine parity + variant
assertion), run_match, tournament (+ Wilson CIs, deck axis), soak (timeout/crash
robustness), tune (coordinate descent), bundle (single-file freeze).
"""
